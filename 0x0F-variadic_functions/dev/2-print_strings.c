#include "variadic_functions.h"
/**
 * print_strings - prints input strings followed by new line
 * @sparator: string to separate each printed string
 * @n: number of strings
 * Return: void
 */
void print_strings(const char *separator, const unsigned int n, ...)
{
	return ();
}
