#include "holberton.h"
/**
 * _strspn - input
 * @s: arg
 * @accept: arg
 * Return: unsigned int
 */
unsigned int _strspn(char *s, char *accept)
{
	unsigned int i;

	while (i < n)
	{
		*(s + i) = b;
		i++;
	}
	return (s);
}
