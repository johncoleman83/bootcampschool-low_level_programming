#include "holberton.h"
/**
 * _strpbrk - input
 * @s: arg
 * @accept: arg
 * Return: pointer to s string
 */
char *_strpbrk(char *s, char *accept)
{
	unsigned int i;

	while (i < n)
	{
		*(s + i) = b;
		i++;
	}
	return (s);
}
