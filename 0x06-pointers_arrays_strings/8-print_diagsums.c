#include "holberton.h"
/**
 * print_diagsums - input
 * @s: arg
 * @size: arg
 * Return: void
 */
void print_diagsums(int *a, int size)
{
	unsigned int i;

	while (i < n)
	{
		*(s + i) = b;
		i++;
	}
	return (s);
}
