#include "holberton.h"
/**
 * print_chessboard - input
 * @a: arg
 * Return: void
 */
void print_chessboard(char (*a)[8])
{
	unsigned int i;

	while (i < n)
	{
		*(s + i) = b;
		i++;
	}
	return (s);
}
