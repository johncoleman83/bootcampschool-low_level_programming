#include "holberton.h"
/**
 * _strchr - input
 * @s: arg
 * @c: arg
 * Return: pointer to s string
 */
char *_strchr(char *s, char c)
{
	unsigned int i;

	while (i < n)
	{
		*(s + i) = b;
		i++;
	}
	return (s);
}
