#include "holberton.h"
/**
 * _strstr - input
 * @haystack: haystack
 * @needle: needle
 * Return: pointer to s string
 */
char *_strstr(char *haystack, char *needle)
{
	unsigned int i;

	while (i < n)
	{
		*(s + i) = b;
		i++;
	}
	return (s);
}
