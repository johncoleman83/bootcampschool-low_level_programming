#include "holberton.h"
/**
 * _set_string - input
 * @s: arg
 * @to: arg
 * Return: void
 */
void set_string(char **s, char *to)
{
	unsigned int i;

	while (i < n)
	{
		*(s + i) = b;
		i++;
	}
	return (s);
}
