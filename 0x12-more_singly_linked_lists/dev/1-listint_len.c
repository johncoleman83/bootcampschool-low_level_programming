#include "lists.h"
/**
 * pistint_len - returns the number of elements in a linked list
 * @h: input head of list
 * Return: number of elements in linked list
 */
size_t listint_len(const listint_t *h)
{
	return ();
}
